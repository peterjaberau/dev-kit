import { defaultConfig } from '@chakra-ui/react';

export default {
  definitions: {
    styles: {
      default: {
        config: defaultConfig,
        prefix: 'default'
      },
    }
  },
} as any
