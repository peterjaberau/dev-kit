
import type { Appearance, CSSColor } from './internal-types';

export const presetStrokeColors: { [TKey in Appearance]: CSSColor } = {
	default: "#1868DB",
	warning: "#E06C00",
};

export const presetStrokeWidth = "2px";
