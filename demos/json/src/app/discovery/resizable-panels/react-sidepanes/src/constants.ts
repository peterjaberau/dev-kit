/**
 * The default width of the left and right sidepanes, in pixels.
 */
export const DEFAULT_SIDE_PANE_WIDTH_IN_PX = 320

/**
 * The maximum width of the central pane, in pixels.
 */
export const CENTRAL_PANE_MAX_WIDTH_IN_PX = 800

/**
 * The minimum width of the central pane, in pixels.
 */
export const CENTRAL_PANE_MIN_WIDTH_IN_PX = 400

/**
 * The minimum width of a side pane, in pixels.
 */
export const MIN_SIDE_PANE_WIDTH_IN_PX = 200

/**
 * Width to apply when the pane is in compact mode.
 */
export const COMPACT_SIDE_PANE_WIDTH_IN_PX = 40

/**
 * The duration of the animation when the sidepane opens, in milliseconds.
 */
export const SIDE_PANE_OPEN_ANIMATION_DURATION_MS = 200

/**
 * Width of the resize handle in pixels.
 */
export const RESIZE_HANDLE_WIDTH_IN_PX = 6

/**
 * Maximum width of the edge sensor in pixels.
 */
export const EDGE_SENSOR_MAX_WIDTH_IN_PX = 200

/**
 * Minimum width of the edge sensor in pixels.
 */
export const EDGE_SENSOR_MIN_WIDTH_IN_PX = 0

/**
 * Cool-down period after a pane is closed during which hovering over the edge should not reopen it.
 */
export const REOPEN_COOLDOWN_MS = 500

/**
 * Padding/dead zone for the right edge sensor in pixels.
 */
export const EDGE_SENSOR_TRIGGER_PADDING_IN_PX = 50

/**
 * Default z-index for the sidepane.
 */
export const SIDEPANE_Z_INDEX = 1200

/**
 * Default z-index for the edge sensor (above sidepane).
 */
export const EDGE_SENSOR_Z_INDEX = 1202

/**
 * Default z-index for the toggle button.
 */
export const TOGGLE_BUTTON_Z_INDEX = 1299
