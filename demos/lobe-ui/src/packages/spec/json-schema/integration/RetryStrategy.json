{
  "$ref": "#/definitions/RetryStrategy",
  "definitions": {
    "RetryStrategy": {
      "type": "string",
      "enum": [
        "exponential_backoff",
        "linear_backoff",
        "fixed_delay",
        "no_retry"
      ],
      "description": "Retry strategy"
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}