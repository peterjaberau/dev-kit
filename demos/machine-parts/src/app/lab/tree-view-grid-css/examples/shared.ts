export const defaultExpandedIds = ["line-a", "line-a/frame", "line-a/drive"];

export const defaultSelectedId = "line-a/drive/encoder";

export type TreeViewExampleProps = {
  expandOnNodeClick: boolean;
  fullRowHighlight: boolean;
};
